# Rhythm Massage 现代 UI 设计指导指南 (Breathing Space) v3.0 - 用户友好修正版

此文档为开发团队进行 UI 重构时的核心设计规范。
> **v3.0 更新注记：** 在保留极简暗黑美学的基础上，引入了「可用性防呆底线 (Usability Guardrails)」，以解决极致无边框设计带来的认知负担和学习成本问题。

---

## 1. 核心设计哲学：呼吸空间 (Breathing Space) - 带护栏版

*   **克制至上，但不隐形 (Reduced, Not Invisible)：** 减少干扰，但关键操作的入口/暗示必须保留。 
*   **去边框化 (Borderless)：** 使用留白 (Spacing) 和背景差来切分层级，告别实线边框。
*   **引导式手势优先 (Guided Gestures)：** 引入盲操全屏手势，但在初次使用或唤醒态下，必须提供强有力的视觉引导。

## 2. 页面结构与渐进式层级 (Progressive Hierarchy)

应用按深度划分为三个层级：
1.  **Main Stage (绝对主视界 - Z0)：** 内容铺满首屏，但保留垂直滚动的可能性。避免页面变成干瘪的单页卡牌。
2.  **Floating Panel (浮动看板 - Z10)：** 从底部滑出，使用 `backdrop-blur-2xl`，顶部提供显性的横线把手 (Handle)，暗示可向下拖拽关闭。
3.  **Deep Settings (深层设定 - Z20)：** 全屏弹出，直接呈现数据与基础选框，放弃花哨动效。

## 3. 设计系统映射与可用性底线 (Design Tokens & WCAG Limits)

严禁在代码中写死魔法数值。以下设定兼顾了美学与“易读性”。

### 3.1 颜色阶梯 (防强光晕眩与防字迹模糊)
*   **主底色:** `bg-black` (`#000000`)
*   **卡片底色:** 弱 `bg-white/[0.04]`，Hover态 `bg-white/[0.08]`。
*   *(修正)* **文字阶梯防线 (Text Scale Limit):**
    *   主标题: `text-white/95`。
    *   正文阅读: `text-white/70` (此前方案过暗，微光下极易疲劳)。
    *   提示图标/辅助字: `text-white/40` **(绝对透明度底线，低于此值在 OLED 黑底上不可读)**。

### 3.2 字体排版 (Typography)
*   **巨大标题引流:** 首屏标题可放大至 `text-5xl font-black`。
*   *(修正)* **废除过细正文:** 黑暗模式下细体字会被背景光晕“吃掉”。所有小于 `text-sm` 的文字，字重保底为 `font-medium`。放弃 `font-light`。

---

## 4. 关键场景重构红线规范 (Page-by-Page Specs)

### 4.1 首页 Dashboard
*   **留白与滑动暗示：** 首页头部可大面积留白渲染情绪。向下垂直滑动展示功能模块。
*   *(修正)* **防死锁的横向流：** 用于推荐播放的横向列表必须强制斩断最后一张卡片。使用固定宽度组合，如 `w-[85vw] snap-center flex-shrink-0`，确保屏幕最右侧**永远**能看到下一张卡片的 15%，引发用户的滑动本能。

### 4.2 沉浸播放器 (The Sanctuary) - 解决手势冲突
播放器分为三态：
1.  **息屏态：** 纯黑或弱呼吸光圈。*(修正)* 保留边缘 5% 透明度的微光刻度线，提供潜意识空间感。
2.  **唤醒态：** 单击屏幕唤起，50% 透明度的控制器浮现，3秒无操作褪去。
3.  *(修正)* **分屏控制态 (Split-Screen Gestures)：**
    为防止“音量”与“力度”调节冲突：
    *   **左侧 50% 屏幕：** 上下滑动调节 **音量** (同步浮现 🔊 图标与读数)。
    *   **右侧 50% 屏幕：** 上下滑动调节 **强度** (同步浮现 📳 图标与读数)。
    *   **双击中央：** 播放/暂停 (伴随水波纹及震动反馈)。

---

## 5. 交互闭环与新手引导 (Feedback & Onboarding)

极致简约的前提是用户“知道系统在干什么”。

### 5.1 触觉反馈标准 (Haptic Hooks)
盲操必须由震动引擎托底：
*   **微过冲 (Tick- 15ms):** 横滑列表卡片对齐时，或者音量/力度越过某个整数区间时。
*   **状态切换 (Success- 30,50,30ms):** 暂停/播放成功响应。
*   **极限边缘 (Bump- 60ms 强震):** 达到最大音量、0力度、列表底部等拒绝响应时。

### 5.2 强制破冰引导 (Mandatory Onboarding Overlay)
*(修正)* **严禁直接把初次使用的用户丢进全黑无按钮的播放器。**
在首次进入 “The Sanctuary” 时：
1.  播放器必须叠加一个半透明的全屏指引罩(`bg-black/60`)。
2.  用高亮的白色虚线与动画演示“左侧滑动=声音👆”，“右侧滑动=力度👆”，“双击=播放⏸️”。
3.  用户完成一次“双击”动作后，遮罩永久消失，正式开启极简模式。
