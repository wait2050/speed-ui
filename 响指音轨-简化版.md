# 响指音轨 — 需求与方案（简化版 v2）

> 上一版（`动态生成响指音频.md`）走偏了：5min 滚动窗口 + 多 source 拼接 → 越改越乱。
> 重做时回归需求本质：**合成一条静音频，并行播放**。

## 需求

1. **运行时动态生成**：根据每阶段配置的响指次数，合成一条响指轨。
2. **与节拍器并行**：共享 `AudioContext` 时钟，独立 `GainNode` 控音量。
3. **随机性 + 稳定性**：用 seeded RNG（Mulberry32）锁定一次 `seed`，pause/resume/seek 复用。
4. **可换源**：用户上传音频可替换 `snapBuffer`（限制 5MB、base64 持久化到 localStorage）。
5. **同步**：暂停/恢复/seek 响指轨保持与主轨一致位置。
6. **整条 timeline 一次拼完**：无长度上限、无滚动窗口。内存换稳定性。

## 解决方案（最简版）

### 核心三件套

| 组件 | 作用 |
| --- | --- |
| `snapBuffer: AudioBuffer` | 音源样本（来自 `snap.mp3` 或用户上传） |
| `snapTrackBuffer: AudioBuffer` | **一条** 拼接后的响指轨（静音底版 + snap 样本嵌入） |
| `snapTrackSource: AudioBufferSourceNode` | **唯一** 播放源，`start(0, offsetSec)` |

### 关键流程

```
start(timeline) →
  ├─ 锁定 snapSeed（一次性）
  ├─ 调 generateSnapTimes() 算出所有 snap 时刻（ms）
  ├─ 调 buildSnapTrackBuffer() 拼一条整轨（长度 = 整条 timeline，无上限）
  └─ startSnapSource(0)

pause() → stopSnapSource()（保 buffer）
resume() → startSnapSource(elapsedBeforePause)
seek(ms) → startSnapSource(ms)         ← 这就是核心：start(0, offsetSec) 物理上不可能"立刻响指"
trigger*() → snapTrackBuffer = null; startSnapSource(this.elapsed)  ← 强制重建
setSnapConfig() / setSnapSource() → snapTrackBuffer = null; startSnapSource(...)
```

### 拼接实现（`buildSnapTrackBuffer`）

```ts
const sr = this.ctx.sampleRate;              // 必须与 snapBuffer.sampleRate 一致
const totalMs = timeline总长;                // 整条 timeline，无上限
const track = ctx.createBuffer(1, ceil(totalMs/1000*sr), sr);
const data = track.getChannelData(0);        // AudioBuffer 初始全 0 = 静音底版

const snapData = snapBuffer.getChannelData(0);
for (const t of snapTimes) {
  if (t >= totalMs) continue;                // 防御性：超出 timeline 范围不嵌
  const offset = Math.floor(t/1000*sr);
  const end = min(totalSamples, offset + snapData.length);
  for (let i = offset; i < end; i++) {
    data[i] += snapData[i - offset];         // 累加而不是覆盖（多次叠加安全）
  }
}
```

### 启动 / 重启播放（`startSnapSource`）

```ts
const offsetSec = clamp(offsetMs/1000, 0, buffer.duration - 0.001); // 物理防越界
stopSnapSource();                                                    // 旧 source stop
const src = ctx.createBufferSource();
src.buffer = snapTrackBuffer;                                        // 复用 buffer
src.connect(snapMasterGain);
src.start(0, offsetSec);                                             // ← sample-accurate
```

## 为什么不用 5min 滚动窗口

- 用户认为 20min × 212MB 内存可接受，换更稳定的实现。
- 滚动窗口（5min 一段，到了再续接）正是上一版出 bug 的根源：续接时间点容易出现"立刻响指"、状态不一致。
- 整条拼完 → 只有一条 source → 不会出"过去时间播放"的问题。

## 反 bug 设计

| 之前的问题 | 现在的保证 |
| --- | --- |
| seek 时立刻响指 | `start(0, offsetSec)` 物理上只能从未来位置开始 |
| 多源调度不同步 | **单 source** 独占整条轨 |
| 拼接轨与 snapBuffer 采样率不一致导致变调 | 都用 `ctx.sampleRate` |
| 5min 续接导致再次"立刻响指" | 没有续接 |
| 复杂状态（cachedSnapTimes、window 长度、startSec） | 一条 `snapTrackBuffer` 足够 |

## 文件

- `src/engine/PlaybackEngine.ts` — 唯一持有响指逻辑的文件
  - `buildSnapTrackBuffer()` 拼一条
  - `startSnapSource(offsetMs)` 启/重启用一个
  - `stopSnapSource()` / `disposeSnapTrack()` 收尾
  - `start/pause/resume/seek/trigger*` 各自只调一次 `startSnapSource`
