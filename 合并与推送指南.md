# 合并与推送指南

## 日常流程

```bash
# 1. 切换到 main 分支
git checkout main

# 2. 拉取最新代码
git pull origin main

# 3. 合并你的功能分支（以 feat/subjective-climax 为例）
git merge feat/subjective-climax

# 4. 如果有冲突，解决后：
git add .
git commit -m "merge: 解决冲突"

# 5. 构建
npm run build

# 6. 替换部署文件
rm -rf docs && mv dist docs

# 7. 提交构建产物
git add -A
git commit -m "build: 合并后构建"

# 8. 推送
git push origin main
```

## 一行搞定（无冲突时）

```bash
git checkout main && git pull origin main && git merge feat/subjective-climax && npm run build && rm -rf docs && mv dist docs && git add -A && git commit -m "build: 合并构建" && git push origin main
```

## 创建新功能分支

```bash
git checkout -b feat/你的功能名
# ... 写代码 ...
git add -A && git commit -m "feat: 描述"
git push origin feat/你的功能名
```

## 回退

```bash
# 回退到上一个稳定版本标签
git checkout v9-stable
npm run build
rm -rf docs && mv dist docs
git checkout main
git add -A && git commit -m "rollback: 回退到 v9-stable" && git push origin main
```

## 常用命令

| 操作 | 命令 |
|---|---|
| 查看当前分支 | `git branch` |
| 查看未提交修改 | `git status` |
| 查看提交历史 | `git log --oneline -5` |
| 放弃本地修改 | `git checkout -- .` |
| 查看两个分支差异 | `git log main..feat/xxx` |
