# 节奏按摩引导器 · 产品方案 V3.1

## 一、项目定位

一款**移动端优先**的智能节拍引导 H5 应用。基于「刺激→休息→再刺激」生理循环模型，自动编排动作序列并按序播放节拍音与语音提示。全程零外部依赖，Web Audio API 实时合成全部音效。

---

## 二、现实目标

| 维度 | 目标 |
|---|---|
| **用户量** | 个人工具，无需规模化 |
| **平台** | 移动端浏览器（iOS Safari / Android Chrome） |
| **离线** | 不需要（依赖 GitHub Pages 托管） |
| **性能** | 音频调度精度 ±5ms，UI 不卡顿 |
| **维护** | 单人可维护 |

---

## 三、核心功能（已实现 ✅ / 规划中 📋）

### 编排引擎
| 功能 | 状态 |
|---|---|
| 基于 6 条约束规则的自动序列编译 | ✅ |
| 热身→核心→冲刺→高潮→余韵→收尾 阶段自动分配 | ✅ |
| 阶段可独立开关（跳过不需要的阶段） | ✅ |
| 动作可独立勾选（过滤不想要的动作） | ✅ |
| 高潮/余韵时长可调（1-5 分钟） | ✅ |
| 锁定动作（重新编排时保持不变） | ✅ |
| 替换动作 | ✅ |
| 打响指随机插入（每阶段次数可配） | ✅ 已替换为真实录音 |
| 自定义动作（用户输入自己的动作名） | 📋 |

### 播放体验
| 功能 | 状态 |
|---|---|
| 6 种合成节拍音（嗒/木鱼/心跳/水滴/敲击/鼓点） | ✅ |
| 4 档速度自定义（慢/中/快/极速，BPM 可调） | ✅ |
| 7 个 TTS 动作语音引导（动作切换时播报，精简版） | ✅ |
| 语音播报期间暂停节拍（播完再打拍） | ✅ |
| 阶段切换信号音（叮/叮叮/重鼓） | ✅ |
| 全局进度条（分阶段色块 + 点击跳转） | ✅ |
| 暂停/继续/停止 | ✅ |
| **🌌 暗夜盲视模式**（播放时纯黑背景 + UI 弱化） | ✅ |
| **🔥 边缘呼吸霓虹光晕**（高潮阶段火红色 box-shadow 动画） | ✅ |
| **⚡ 狂热打点**（左滑 / ← 键触发空灵风铃 + 振动 + 红色水波纹） | ✅ |
| **🔥 主观高潮控制**（右滑到底 / → 键瞬间跳转无限高潮冲刺，135BPM） | ✅ |
| **✨ 释放余韵三部曲**（再次右滑进入余韵→收尾→静默着陆） | ✅ |
| 背景音频（本地文件循环播放） | 📋 |
| 屏幕常亮（Wake Lock） | ✅ |

### 数据管理
| 功能 | 状态 |
|---|---|
| 编排历史（最近 20 条） | ✅ |
| 收藏编排 + 标签 | ✅ |
| 使用统计（累计次数/时长/评分） | ✅ |
| 编排导出为 JSON 文件 | ✅ |
| 从 JSON 文件导入编排 | ✅ |
| 播放进度恢复（2 小时内中断可续） | ✅ |
| 偏好记忆（速度/音色/时长） | ✅ |
| **狂热打点毫秒级日志**（独立存储，不随引擎重建丢失） | ✅ |
| **历史详情全屏页**（热力图 + 阶段分布 + 打点明细 + AI 指令） | ✅ |

### 界面
| 功能 | 状态 |
|---|---|
| 侧边栏（历史/收藏/统计/导入） | ✅ |
| 编排预览（统计 + 可编辑序列列表 + 休息显示） | ✅ |
| 播放页（动作名 + 倒计时 + 进度条 + 按键 + 底部盲控滑块） | ✅ |
| 静默着陆页（呼吸灯 + 评分 + 导出按钮） | ✅ |
| 历史详情全屏页（热力图 + 阶段甘特条 + 打点时间线 + AI 迭代指令） | ✅ |
| 暗色主题，移动端适配 | ✅ |
| 页面底部静谧介绍 | ✅ |

---

## 四、当前架构

```
src/
├── engine/
│   └── PlaybackEngine.ts        # 统一音频引擎 + 狂热打点 + 主观高潮跳转
├── compiler/                    # 序列编译器（纯函数 + 单元测试）
│   ├── compiler.ts              # 主编译逻辑
│   ├── rules.ts                 # 约束规则常量
│   ├── pools.ts                 # 动作池定义
│   └── __tests__/
│       └── compiler.test.ts     # 13 个单元测试
├── pages/
│   ├── Home.tsx                 # 首页（时长/阶段/动作/速度配置）
│   ├── Preview.tsx              # 编排预览（可编辑序列）
│   ├── Player.tsx               # 播放页（暗夜盲视 + 霓虹 + 盲控滑块）
│   ├── Landing.tsx              # 静默着陆（呼吸灯 + 评分 + 导出）
│   └── HistoryDetail.tsx        # 历史详情全屏页（热力图 + AI 指令）
├── components/
│   ├── Timer.tsx                # 圆形倒计时
│   ├── ProgressBar.tsx          # 阶段色块进度条
│   ├── Sidebar.tsx              # 侧边栏（历史/收藏/统计/导入）
│   ├── SequenceList.tsx         # 只读序列列表
│   ├── BreathingLight.tsx       # 呼吸灯动画
│   └── Footer.tsx               # 底部静谧介绍
├── state/
│   └── store.ts                 # Zustand 全局状态
├── storage/
│   ├── index.ts                 # localStorage 操作
│   └── export.ts                # 编排导出/导入
├── audio/
│   └── sounds.ts                # 音频合成函数（节拍音 + 风铃 + 信号音）
├── styles/
│   ├── global.css               # CSS 变量 + Reset + 动画
│   ├── buttons.css              # 通用按钮
│   ├── home.css                 # 首页样式
│   ├── preview.css              # 预览 + 序列编辑
│   ├── player.css               # 播放页 + 盲控滑块 + 霓虹光晕
│   ├── landing.css              # 着陆页
│   ├── sidebar.css              # 侧边栏
│   ├── footer.css               # 底部介绍
│   └── responsive.css           # 响应式
├── utils/
│   └── time.ts                  # 时间格式化工具
└── types/
    └── index.ts                 # 全局类型定义
```

### 架构特点
- **PlaybackEngine** 是 class 实例，存储在 useRef 中，不受 React 渲染影响
- **编译器**是纯函数，13 个 Vitest 单元测试覆盖
- **Zustand** 替代了 Context+Reducer，无 Provider 嵌套
- **CSS 拆分为 9 个模块文件**，index.css 仅作聚合入口
- 旧 scheduler/ 目录已清理
- `setOnUpdate` / `setOnFinished` 单向推送 UI 状态

---

## 五、已完成的重构

| Phase | 内容 | 状态 |
|---|---|---|
| Phase 1 | 删除旧 scheduler/clock.ts + scheduler/scheduler.ts，移动 time 工具到 utils/ | ✅ |
| Phase 1 | 编译器 13 个单元测试（Vitest） | ✅ |
| Phase 2 | Zustand 替换 Context/Reducer | ✅ |
| Phase 2 | CSS 拆分为 9 个模块文件 | ✅ |
| Phase 3 | 暗夜盲视 + 呼吸霓虹 + 狂热打点 + 主观高潮控制 | ✅ |
| Phase 3 | 历史详情全屏页（热力图 + AI 指令） | ✅ |
| Phase 3 | 响指录音替换合成 | ✅ |
| Phase 3 | 兴奋点独立存储 | ✅ |

---

## 六、产品边界

**本产品提供**
- 基于生理模型的智能动作编排
- Web Audio API 合成节拍音 + TTS 语音引导
- 狂热打点与主观高潮控制
- 历史详情分析与 AI 迭代指令
- 本地偏好学习与编排记忆
- 编排导出/导入

**本产品不提供**
- 语音助手 / AI 对话
- 云端同步
- 社交分享
- 医学建议

---

*文档版本 V3.1 · 更新日期 2026-06-03*
