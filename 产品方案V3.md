# 节奏按摩引导器 · 产品方案 V3.0

## 一、项目定位

一款**移动端优先**的智能节拍引导 H5 应用。基于「刺激→休息→再刺激」生理循环模型，自动编排动作序列并按序播放节拍音与语音提示。全程零外部依赖，Web Audio API 实时合成全部音效。

---

## 二、现实目标

| 维度 | 目标 |
|---|---|
| **用户量** | 个人工具，无需规模化 |
| **平台** | 移动端浏览器（iOS Safari / Android Chrome） |
| **离线** | 不需要（依赖 GitHub Pages 托管） |
| **性能** | 音频调度精度 ±5ms，UI 不卡顿 |
| **维护** | 单人可维护，代码量控制在 5000 行内 |

---

## 三、核心功能（已实现 ✅ / 规划中 📋）

### 编排引擎
| 功能 | 状态 |
|---|---|
| 基于 6 条约束规则的自动序列编译 | ✅ |
| 热身→核心→冲刺→高潮→余韵→收尾 阶段自动分配 | ✅ |
| 阶段可独立开关（跳过不需要的阶段） | ✅ |
| 动作可独立勾选（过滤不想要的动作） | ✅ |
| 高潮/余韵时长可调（1-5 分钟） | ✅ |
| 锁定动作（重新编排时保持不变） | ✅ |
| 替换动作 | ✅ |
| 打响指随机插入（每阶段次数可配） | 🐛 音效待替换 |
| 自定义动作（用户输入自己的动作名） | 📋 |

### 播放体验
| 功能 | 状态 |
|---|---|
| 6 种合成节拍音（嗒/木鱼/心跳/水滴/敲击/鼓点） | ✅ |
| 4 档速度自定义（慢/中/快/极速，BPM 可调） | ✅ |
| 14 个 TTS 动作语音引导（动作切换时播报） | ✅ |
| 语音播报期间暂停节拍（播完再打拍） | ✅ |
| 阶段切换信号音（叮/叮叮/重鼓） | ✅ |
| 全局进度条（分阶段色块 + 点击跳转） | ✅ |
| 暂停/继续/停止 | ✅ |
| 背景音频（本地文件循环播放） | 📋 |
| 屏幕常亮（Wake Lock） | ✅ |

### 数据管理
| 功能 | 状态 |
|---|---|
| 编排历史（最近 20 条） | ✅ |
| 收藏编排 + 标签 | ✅ |
| 使用统计（累计次数/时长/评分） | ✅ |
| 编排导出为 JSON 文件 | ✅ |
| 从 JSON 文件导入编排 | ✅ |
| 播放进度恢复（2 小时内中断可续） | ✅ |
| 偏好记忆（速度/音色/时长） | ✅ |

### 界面
| 功能 | 状态 |
|---|---|
| 侧边栏（历史/收藏/统计/导入） | ✅ |
| 编排预览（统计 + 可编辑序列列表） | ✅ |
| 播放页（动作名 + 倒计时 + 进度条 + 按键） | ✅ |
| 静默着陆页（呼吸灯 + 评分） | ✅ |
| 暗色主题，移动端适配 | ✅ |
| 页面底部静谧介绍 | ✅ |

---

## 四、架构现状与优化方向

### 当前架构

```
src/
├── engine/PlaybackEngine.ts    # 统一音频引擎（class）
├── compiler/                    # 序列编译器（纯函数）
├── pages/                       # 页面组件（Home/Preview/Player/Landing）
├── components/                  # 通用组件（Sidebar/Timer/ProgressBar/Footer）
├── state/                       # 全局路由状态（reducer+context）
├── storage/                     # localStorage 操作 + 导出/导入
├── audio/                       # 音频合成函数（旧，仍被引用）
├── scheduler/                   # 旧调度器（已废弃，Player 改用 PlaybackEngine）
└── types/                       # 类型定义
```

### 架构优点（保留）
- PlaybackEngine 是 class 实例，存储在 useRef 中，不受 React 渲染影响 ✅
- 编译器是纯函数，可独立测试 ✅
- 引擎通过回调单向推送 UI 状态 ✅

### 架构问题（需修复）
| 问题 | 影响 | 优先级 |
|---|---|---|
| 旧 `audio/` 和 `scheduler/` 目录未清理 | 混淆，增加维护成本 | 高 |
| `types/index.ts` 膨胀到 160+ 行 | 难以定位 | 中 |
| 编译器 `compiler.ts` 380+ 行 | 单文件过长 | 中 |
| 无单元测试 | 改编译器容易引入 Bug | 中 |
| CSS 单文件 1000+ 行 | 难以维护 | 低 |

### 参考项目的最佳实践

**drumhaus (142⭐, React + Tone.js)**
- 按功能分层：`core/`（引擎）、`features/`（功能模块）、`shared/`（共享组件）
- 动画时钟统一管理 UI 刷新（`requestAnimationFrame`），不通过 React state 驱动高频更新
- 音频引擎完全独立于 React 渲染周期

**beatsync (3k⭐, TypeScript)**
- 精确时钟同步是核心卖点
- Web Audio `currentTime` 作为唯一时间源
- 最小化依赖

**通用模式总结**
1. 音频引擎 = 独立 class，通过 ref 持有
2. 时间 = `audioContext.currentTime` 唯一真理源
3. UI 更新 = 低频回调（50ms）推送到 React state
4. 状态管理 = 轻量方案（Zustand/Jotai）优于 Context+Reducer
5. 移动端 = 触摸目标 ≥44px、避免 `user-scalable=no`、AudioContext 在用户手势中恢复

---

## 五、V3.0 重构计划

### Phase 1：清理 + 测试（1-2 天）
- [ ] 删除旧 `audio/engine.ts` 和 `scheduler/scheduler.ts`（已不再使用）
- [ ] 删除旧 `scheduler/clock.ts`（合并到 engine）
- [ ] 编译器核心逻辑加单元测试（Vitest）
- [ ] 拆分 `compiler.ts`（编译 + 插入 + 验证各自独立文件）

### Phase 2：架构优化（2-3 天）
- [ ] 拆分 `types/index.ts` 为 `types/sequence.ts`、`types/state.ts`、`types/prefs.ts`
- [ ] CSS 按组件拆分（CSS Modules 或 styled-components）
- [ ] 替换 Context+Reducer 为 Zustand（更轻，无 Provider 嵌套）
- [ ] PlaybackEngine 加 `requestAnimationFrame` 驱动 UI 进度条更新（替代 50ms setInterval 推 React state）

### Phase 3：功能完善（按需）
- [ ] 真实响指录音替换合成
- [ ] 背景音频（本地文件）
- [ ] 自定义动作（用户输入）
- [ ] iOS 静音开关检测提示
- [ ] Service Worker 缓存策略（解决每次清缓存问题）

---

## 六、产品边界

**本产品提供**
- 基于生理模型的智能动作编排
- Web Audio API 合成节拍音 + TTS 语音引导
- 本地偏好学习与编排记忆
- 编排导出/导入

**本产品不提供**
- 语音助手 / AI 对话
- 云端同步
- 社交分享
- 医学建议

---

*文档版本 V3.0 · 编写日期 2026-06-02*
